PROTECTED (AEvo)

  port.ts   — the Logger contract. May add a method/level; may not drop one.
  gates.ts  — may ADD a gate / RAISE a threshold / ADD a requirement; may not remove or lower.

Enforced structurally by gates.assertNoLoosening. adapters/*, demo.ts, insights.md are free.
