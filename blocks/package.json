{
  "name": "substrate-blocks",
  "private": true,
  "type": "module",
  "description": "The nursery: runnable capability blocks. App code imports each block's index.ts only.",
  "scripts": {
    "demo": "node persistence/demo.ts && node env/demo.ts && node logging/demo.ts && node transport/demo.ts && node nursery-app/main.ts",
    "test": "node persistence/block.test.ts && node env/block.test.ts && node logging/block.test.ts && node transport/block.test.ts && node nursery-app/app.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
